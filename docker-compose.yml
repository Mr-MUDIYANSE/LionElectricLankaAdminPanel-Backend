services:
  lion_lanka_server:
    build: .
    ports:
      - "5000:4000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL="postgresql://neondb_owner:npg_uB7eFwrTfVx4@ep-broad-glitter-a181m5ob-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
      - PORT="4000"
    command: sh -c "npx prisma migrate dev --name init && npm start"